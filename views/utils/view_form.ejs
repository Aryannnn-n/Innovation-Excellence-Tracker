<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Submission Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-blue-200 flex items-center justify-center min-h-screen p-4">
    <div class="bg-blue-50 p-8 rounded-xl shadow-lg w-full max-w-2xl">
      <h2 class="text-3xl font-semibold text-gray-800 text-center mb-6">
        Viewed Form
      </h2>

      <!-- Display Form -->
      <div class="space-y-6">
        <div>
          <label class="block font-medium text-gray-700">Title:</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.title %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700">Category:</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.category %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700">Department:</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.department %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700">Description:</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.description %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700">Key Features:</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.keyFeatures %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700">Collaborators:</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.collaborators %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700">Mentor(s):</label>
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.mentors %>
          </p>
        </div>

        <div>
          <label class="block font-medium text-gray-700"
            >Impact & Future Scope:</label
          >
          <p
            class="w-full p-3 mt-1 bg-blue-200 border border-gray-300 rounded-md"
          >
            <%= adminUser.info %>
          </p>
        </div>

        <button
          type="button"
          onclick="window.history.back()"
          class="w-full mt-6 bg-blue-600 text-white p-3 rounded-md hover:bg-blue-700 transition text-lg font-semibold"
        >
          Done
        </button>
      </div>
    </div>
  </body>
</html>
